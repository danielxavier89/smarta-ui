import { Meta, Markdown } from "@storybook/addon-docs/blocks";
import { docsFrom } from "@/lib/docs";
import source from "../../../../packages/ui/docs/recipes/list-page.md?raw";

{/*
  Rendered from the file itself, not retyped. The commonest screen in both products. Start here rather than assembling from 34 parts.
*/}

<Meta title="Recipes/A list page" />

# A list page

<Markdown>{docsFrom(source)}</Markdown>
