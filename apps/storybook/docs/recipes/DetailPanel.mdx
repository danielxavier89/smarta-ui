import { Meta, Markdown } from "@storybook/addon-docs/blocks";
import { docsFrom } from "@/lib/docs";
import source from "../../../../packages/ui/docs/recipes/detail-panel.md?raw";

{/*
  Rendered from the file itself, not retyped. What opens when a row is clicked.
*/}

<Meta title="Recipes/A detail panel" />

# A detail panel

<Markdown>{docsFrom(source)}</Markdown>
