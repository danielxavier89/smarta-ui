import { Meta, Markdown } from "@storybook/addon-docs/blocks";
import { docsFrom } from "@/lib/docs";
import source from "../../../../packages/ui/docs/recipes/form.md?raw";

{/*
  Rendered from the file itself, not retyped. Including validation timing, which is the part everyone gets wrong.
*/}

<Meta title="Recipes/A form" />

# A form

<Markdown>{docsFrom(source)}</Markdown>
